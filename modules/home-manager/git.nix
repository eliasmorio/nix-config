# Git configuration shared across all hosts
_:

{
  programs.git = {
    enable = true;
    settings.user = {
      name = "eliasmorio";
      email = "eliasmorio@users.noreply.github.com";
    };
  };
}
